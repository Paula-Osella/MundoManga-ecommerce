<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Mundo Manga Online</title>

  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"/>
</head>
<body
  data-username="{{username}}"
  data-cart-id="{{cartId}}"
>
  <header>
    <a href="/products" class="header-logo-link">
      <h1>Mundo Manga</h1>
    </a>
    <h2>Tienda de Mangas Online</h2>
    <nav>
      <a href="{{#if cartId}}/carts/{{cartId}}{{else}}#{{/if}}" id="cartLink">
        <i class="fas fa-shopping-cart"></i> Ver carrito
        <span id="cartCountBadge" style="display:none; margin-left:6px; padding:2px 8px; border-radius:999px; background:#fff; color:#E74C3C; font-weight:700; font-size:12px;">0</span>
      </a>

      {{#if username}}
        <a href="/logout" id="logoutLink"><i class="fas fa-sign-out-alt"></i> Cerrar sesión</a>
      {{else}}
        <a href="/login"><i class="fas fa-user"></i> Login</a>
      {{/if}}
    </nav>
  </header>

  <main>
    {{{body}}}
  </main>



  <footer>
    <p>&copy; 2024 Mundo Manga - Tienda de mangas online - Todos los derechos reservados.</p>
  </footer>

  <!-- Contenedor del toast global -->
  <div id="appToast" class="app-toast" hidden>
    <i class="app-toast__icon fas fa-check-circle" aria-hidden="true"></i>
    <span class="app-toast__text"></span>
  </div>

  <!-- JS externos -->
  <script src="/js/app-toast.js" defer></script>
  <script src="/js/main-layout.js" defer></script>
</body>
</html>
